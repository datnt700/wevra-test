# Tavia Mobile App - Environment Variables
# Mobile app for ATTENDEE users

# ==============================================================================
# EXPO ENVIRONMENT SYSTEM
# ==============================================================================
# Expo supports .env files for local development:
#   .env                  - Default for all environments
#   .env.local            - Local overrides (git ignored)
#   .env.development      - Development mode (npx expo start)
#   .env.production       - Production builds (eas build)
#
# ⚠️ IMPORTANT: All variables exposed to client code MUST have EXPO_PUBLIC_ prefix
# Variables without prefix are only available in app.config.js (build-time)
#
# For EAS Build/Update: Use EAS environment variables instead of .env files
#   - EAS supports 3 environments: development, preview, production
#   - Set in: https://expo.dev/accounts/[account]/projects/[project]/environment-variables
#   - Pull locally: eas env:pull --environment development
#   - Build: eas build --profile production (uses production environment)
#   - Update: eas update --environment production
#
# Learn more:
# - Local .env: https://docs.expo.dev/guides/environment-variables/
# - EAS environments: https://docs.expo.dev/eas/environment-variables/
# ==============================================================================

# ==============================================================================
# API CONFIGURATION
# ==============================================================================
# ⚠️ Mobile auth endpoints are in backoffice (port 3000)
# For physical devices: Use your computer's local IP (same WiFi network)
# Find IP: Windows (ipconfig), Mac/Linux (ifconfig)
EXPO_PUBLIC_API_URL=http://192.168.1.16:3000

# Analytics API (optional - port 3001)
EXPO_PUBLIC_ANALYTICS_API_URL=http://192.168.1.16:3001

# ==============================================================================
# OAUTH PROVIDERS
# ==============================================================================
# Google: Create iOS and Android OAuth clients in Google Cloud Console
# See: docs/OAUTH_QUICKSTART.md for step-by-step guide
EXPO_PUBLIC_GOOGLE_CLIENT_ID=your_google_client_id.apps.googleusercontent.com

# Facebook: Create Facebook app and get App ID
EXPO_PUBLIC_FACEBOOK_CLIENT_ID=your_facebook_app_id

# Apple Sign In Configuration
# ⚠️ Apple doesn't use EXPO_PUBLIC_ variables - configured in app.json
# Make sure these settings are in app.json:
#   "ios": {
#     "usesAppleSignIn": true,
#     "bundleIdentifier": "io.tavia.mobile"
#   }
# Apple credentials are handled server-side (apps/backoffice/src/app/api/mobile/auth/apple/route.ts)

# ==============================================================================
# ANALYTICS
# ==============================================================================
EXPO_PUBLIC_ANALYTICS_API_KEY=your-analytics-api-key-here
EXPO_PUBLIC_ENABLE_ANALYTICS=true

# ==============================================================================
# APP CONFIGURATION
# ==============================================================================
EXPO_PUBLIC_APP_NAME=Tavia Mobile
EXPO_PUBLIC_APP_VERSION=0.1.0

# App Environment (for display purposes only - does not affect EAS environments)
EXPO_PUBLIC_ENVIRONMENT=development

# ==============================================================================
# FEATURE FLAGS
# ==============================================================================
EXPO_PUBLIC_ENABLE_DEBUG=true
EXPO_PUBLIC_ENABLE_PERFORMANCE_MONITORING=false

# ==============================================================================
# BUILD-TIME VARIABLES (app.config.js only - NOT available in client code)
# ==============================================================================
# These variables are used in app.config.js to configure the app at build time
# They do NOT need EXPO_PUBLIC_ prefix and are NOT exposed to client code

# App Variant (for multiple build profiles)
# Used in app.config.js to set bundleIdentifier and package name
# Examples: development, preview, production
APP_VARIANT=development

# Sentry Configuration (build-time only)
# SENTRY_AUTH_TOKEN=your-sentry-auth-token
# SENTRY_ORG=your-sentry-org
# SENTRY_PROJECT=your-sentry-project

# Google Services (build-time only)
# GOOGLE_SERVICES_JSON=/path/to/google-services.json  # Android
# GOOGLE_SERVICES_INFO_PLIST=/path/to/GoogleService-Info.plist  # iOS
