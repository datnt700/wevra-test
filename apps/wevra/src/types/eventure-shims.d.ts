// Shim removed — package now provides proper types and path mappings.
// Keeping this file temporarily in case someone needs it, but it's effectively
// a no-op to avoid affecting the TypeScript environment.

export {};
