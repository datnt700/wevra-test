/**
 * Text Component
 *
 * A styled text component with typography variants and colors.
 *
 * @example
 * ```tsx
 * import { Text } from '@eventure/eventurex';
 *
 * <Text variant="h1" color="primary">
 *   Welcome Back
 * </Text>
 * <Text variant="body" color="secondary">
 *   Sign in to continue
 * </Text>
 * ```
 */

export { Text } from './components';
export type { EventureTextProps, TextVariant, TextColor } from './types';
