/**
 * Button Component
 *
 * A flexible button component for React Native with multiple variants,
 * sizes, and shapes. Supports loading states, icons, and accessibility.
 *
 * @example
 * ```tsx
 * import { Button } from '@eventure/mobile-ui';
 *
 * <Button variant="primary" onPress={() => console.log('Pressed!')}>
 *   Press me
 * </Button>
 * ```
 */

export { Button } from './components';
export type { ButtonProps, ButtonVariant, ButtonShape, ButtonSize } from './types';
