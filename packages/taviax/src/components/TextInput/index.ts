/**
 * TextInput Component
 *
 * A styled text input component for React Native with label, error,
 * and focus states support.
 *
 * @example
 * ```tsx
 * import { TextInput } from '@tavia/taviax';
 *
 * <TextInput
 *   label="Email"
 *   placeholder="Enter your email"
 *   error="Invalid email"
 *   required
 * />
 * ```
 */

export { TextInput } from './components';
export type { TextInputProps } from './types';
