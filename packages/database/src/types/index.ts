/**
 * Shared type definitions for database models
 *
 * Add shared types here that are used across multiple apps.
 * Keep types minimal and focused on what's actually shared.
 */

// Export common types
export * from './common';
