/**
 * Props for the LoadingScreen component.
 *
 * Note: LoadingScreen is a static component with no props.
 * It displays a full-screen loading animation during app initialization.
 */
export interface LoadingScreenProps {
  // This component intentionally has no props.
  // Reserved for future enhancements (e.g., custom logo, message, progress).
}
